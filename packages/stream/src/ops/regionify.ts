import { AbstractOp } from '../op.js';

/**
 * Reserved: registered as `regionify`, but not implemented.
 *
 * Like {@link Heatmap}, it passes the frame through untouched.
 */
export class Regionify extends AbstractOp {}
